wsl --install -d Ubuntu-22.04

python3.10 -m venv venv
py -3.10 -m venv venv
source venv/bin/activate
python -m pip install --upgrade pip

WSL: New Window

# pip 강제 재설치
wget https://bootstrap.pypa.io/get-pip.py -O get-pip.py
python3 get-pip.py

sudo add-apt-repository ppa:deadsnakes/ppa
sudo apt update

sudo apt install python3.10-venv python3.10-dev
python3.10 -m venv venv
source venv/bin/activate

python -m pip install --upgrade pip
pip install "tensorflow==2.15.*" "tensorflow-text==2.15.*" keras-nlp

===========keras2==========
pip install --no-deps tensorflow==2.15.0
pip install --no-deps tensorflow-text==2.15.0
pip install --no-deps tensorflow-hub==0.15.0
pip install --no-deps keras==2.10.0
pip install --no-deps keras-nlp==0.3.1
pip install --no-deps numpy==1.21.6
pip install --no-deps packaging==24.0
pip install --no-deps werkzeug<3
pip install --upgrade keras tensorflow keras_hub keras_nlp
pip install torch --index-url https://download.pytorch.org/whl/cu118





pip install ipykernel
python -m ipykernel install --user --name hm-dl --display-name "Python (WSL hm-dl)"

[Environment]::GetEnvironmentVariable("PATH","Machine")
py -3.10 -m venv .venv
======keras3==============
pip install --no-deps numpy==1.26.4
pip install --no-deps tensorflow==2.16.2
pip install --no-deps tensorflow-hub
pip install --no-deps keras>=3
pip install --no-deps keras-nlp>=0.6
